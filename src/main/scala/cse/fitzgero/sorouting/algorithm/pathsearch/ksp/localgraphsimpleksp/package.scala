package cse.fitzgero.sorouting.algorithm.pathsearch.ksp

import cse.fitzgero.sorouting.algorithm.pathsearch.od.localgraph._
import cse.fitzgero.sorouting.algorithm.pathsearch.od.{ODPair, ODPath}
import cse.fitzgero.sorouting.roadnetwork.localgraph.{EdgeId, VertexId}

package object localgraphsimpleksp {

//  case class LocalGraphODPair(srcVertex: VertexId, dstVertex: VertexId) extends ODPair[VertexId]
//
//  case class LocalGraphODPath(srcVertex: VertexId, dstVertex: VertexId, path: List[EdgeId], cost: List[Double]) extends ODPath[VertexId, EdgeId]
//  object LocalGraphODPath {
//    def from(a: SimpleSSSP_ODPath): LocalGraphODPath =
//      LocalGraphODPath(a.srcVertex, a.dstVertex, a.path, a.cost)
//  }
}
